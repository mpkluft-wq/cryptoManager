{% extends "default_layout/main_layout.html.twig" %}

{% block body %}
<div class="container mt-4">
    <h1>Калькулятор продаж по сетке</h1>

    <div class="card mt-3">
        <div class="card-body">
            {{ form_start(form) }}
            <div class="row">
                <div class="col-md-4">{{ form_row(form.average_price) }}</div>
                <div class="col-md-4">{{ form_row(form.total_asset_amount) }}</div>
                <div class="col-md-4">{{ form_row(form.target_multiplier) }}</div>
            </div>
            <div class="row">
                <div class="col-md-6">{{ form_row(form.rise_step_percent) }}</div>
                <div class="col-md-6">{{ form_row(form.precision) }}</div>
            </div>
            <div class="mt-3">
                {{ form_row(form.calculate) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>

    {% if result %}
        <div class="mt-4">
            <h3>Уровни продаж</h3>
            <div class="table-responsive">
                <table class="table table-sm table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Цена</th>
                            <th>% от общего объёма</th>
                            <th>Объём</th>
                            <th>Ожидаемая выручка</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for row in result.sell_levels %}
                            <tr>
                                <td>{{ row.level }}</td>
                                <td>{{ row.price }}</td>
                                <td>{{ row.percent }}%</td>
                                <td>{{ row.amount }}</td>
                                <td>{{ row.expected_profit }}</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>

            <h3 class="mt-4">Итоги</h3>
            <ul>
                <li>Объём, проданный по сетке: <strong>{{ result.meta.total_sold_amount }}</strong></li>
                <li>Итоговая выручка-цель: <strong>{{ result.meta.target_revenue }}</strong></li>
                <li>Цель достигнута: <strong>{{ result.meta.reached_target ? 'Да' : 'Нет' }}</strong></li>
            </ul>
        </div>
    {% endif %}
</div>
{% endblock %}
